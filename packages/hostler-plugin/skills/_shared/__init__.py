"""skills/_shared — Shared Python utilities across skills.

Skill directories are generally self-contained, but duplicated patterns that
recur in 2+ places are extracted into this module.

Current modules:
    - config_loader: dual-search loader for skill config JSON files
"""
