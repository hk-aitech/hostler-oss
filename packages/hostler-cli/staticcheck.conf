# staticcheck configuration.
#
# Disabled checks:
#   - ST1005/ST1020/ST1021: comment / error-message style preferences that
#     don't reflect a strict spec requirement here.
#
# Everything else (SA*/S*/U*) stays on — real bugs and semantic-logic errors
# are blocked by CI.
checks = ["all", "-ST1005", "-ST1020", "-ST1021"]
